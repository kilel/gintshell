#out{
//case dose not make a sense. This is comment
	goblin : ar'tar, migot, ginuv, hjiz; //goblin.ar'tar = 1, foblin.migot = 2...
	elf : feanor, galadriel, isildur, elrond; //elf.feanor = 4, elf.galadriel = 5...
	alaento, riggor;
	Gandalf;
}

#in{
	//the same
	height: high, middle, low;
}

#rules{
	if( height.high ){
		elf.feanor = 0.7;
		elf.galadriel =  0.6;
		if(...)
			goblin.migot = 0.3;
		
	} elif (height.low){
		//
	}
	else{
		//
	}
	
}
//Штобб, или ярушкин. построение нечётких сетей.
//LVQ, нейронные сети RBF.


Лексемы языка: 
{Лексемы}
выход = знач;
if(входы) лексема

projDir/
	modules/
		module1/
			config
		module2/
			config
		module2/  (productional)
			config
			code
	integrator
	config
	
project config:
	name 


program config:
	path1  (paths to the projects)
	path2
	path3
	

module config:
	type   (neural, hybrid, neural, fuzzy)
	etc..
	
neural module config etc:
	layers in format layer1-layer2-...-layerN
	other parameters  
	